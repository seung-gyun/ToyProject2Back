<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.savemoney.co.kr.mapper.MemberMapper">
    <select id="joinMember" parameterType="com.savemoney.co.kr.dto.MemberDTO">
        
        INSERT INTO MEMBERS(MEMBERID, MEMBERPWD, PHONENUMBER, EMAIL, ADRESS)
        VALUES (#{memberId}, #{memberpwd}, #{phonenumber}, #{email}, #{address})

    </select>
</mapper>